# Frontend Dependencies Only
# Minimal requirements for Streamlit frontend

# Frontend
streamlit==1.30.0
plotly==5.18.0

# API Client
requests==2.31.0

# Data Processing (needed for frontend display)
pandas==2.2.0
numpy==1.26.3

# Utilities
python-dotenv==1.0.0

# Pydantic (for API schemas and config)
pydantic==2.5.3
pydantic-settings==2.1.0

# Database (needed for Research page)
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# Date utilities (needed for Research page)
python-dateutil==2.8.2

# Data Sources (needed for BacktestEngine)
yfinance>=0.2.54
